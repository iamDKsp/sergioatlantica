{
  "name": "workspace",
  "version": "0.0.0",
  "description": "Atlântica Natural Bauru client website",
  "license": "MIT",
  "scripts": {
    "preinstall": "",
    "build": "pnpm --filter @workspace/atlantica-natural-bauru build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
